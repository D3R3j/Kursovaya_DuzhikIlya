<Page x:Class="Kursovaya_DuzhikIlya.pages.InventoryManagmentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Kursovaya_DuzhikIlya.pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Управление инвентаризацией">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Управление актами инвентаризации" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <DataGrid x:Name="InventoryActsGrid" AutoGenerateColumns="False" ItemsSource="{Binding InventoryActs}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Дата начала" Binding="{Binding StartDate}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Дата окончания" Binding="{Binding EndDate}" IsReadOnly="True"/>
                    <DataGridTextColumn Header="Ответственный" Binding="{Binding EmployeeName}" IsReadOnly="True"/>
                    <DataGridTemplateColumn Header="Статус">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Status}" Foreground="{Binding Status, Converter={StaticResource StatusToColorConverter}}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                <Button Content="Создать акт" Background="#2196F3" Foreground="White" Width="150" Margin="0,0,10,0" Click="CreateAct_Click"/>
                <Button Content="Удалить акт" Background="#F44336" Foreground="White" Width="150" Click="DeleteAct_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
